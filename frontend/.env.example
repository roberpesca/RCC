# Base URL of the deployed backend API, including the /api suffix.
# Local dev: leave unset (vite.config.js proxies /api to http://localhost:8080).
# Production (e.g. frontend on Vercel, backend on Render): set to your Render URL.
VITE_API_URL=https://your-backend.onrender.com/api
